name: traccar_distributor
description: Minimal Traccar client (login by phone & device commands)
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
